# Benchmarks

Reproducible performance numbers for Klyvion. Run:

```bash
python benchmarks/bench_synthesis.py
```

Results (fill in per release; hardware matters — always record it):

| Version | Hardware | Engine | Input | Time | RTF* |
|---------|----------|--------|-------|------|------|
| 0.1.0-alpha | _tbd_ | xtts/cpu | 100 chars | _tbd_ | _tbd_ |

*RTF = real-time factor (synthesis time ÷ audio duration; lower is better).
